to run integration test in the root directory:

flutter drive   --driver=test_driver/integration_test.dart   --target=integration_test/topLevel_test.dart

run on chrome:

download chromedriver and install in path: https://flutter.dev/docs/cookbook/testing/integration/introduction#6b-web

start on command line: note port number

other terminal enter command:
flutter drive --driver=test_driver/integration_test.dart \
              --target=integration_test/topLevel_test.dart \
              -d web-server --driver-port=<driverport>

